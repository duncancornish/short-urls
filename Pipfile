[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-sqlalchemy = "*"

[dev-packages]
pytest = "*"

[requires]
python_version = "3"

[scripts]
dev = "bash -c \"export FLASK_DEBUG=true && flask run\""
test = "pytest ."